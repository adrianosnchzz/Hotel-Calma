# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0

# Configuración de compilación para forzar la instalación limpia de dependencias
build:
  buildCommand: npm install --legacy-peer-deps && ng build